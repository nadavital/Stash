export function renderFolderItemGrid() {
  return `
    <section class="folder-item-grid" data-component="folder-item-grid" aria-label="Folder items">
      <div id="folder-items-grid" class="folder-items-grid"></div>
    </section>
  `;
}
